import { Box } from '@mui/material';

export default function QuestFinalPage({ currentQuestEndEvent }: { currentQuestEndEvent: string }) {
	console.log('currentQuestEndEvent :', currentQuestEndEvent);
	return <Box>Quest Final</Box>;
}
